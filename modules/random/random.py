# import is keyword in python, used to import python module into current python file.
# random is python module gives random numbers.
import random

random_value = random.randint(10, 50)
print(random_value)

random_value = random.randint(10, 50)
print(random_value)
